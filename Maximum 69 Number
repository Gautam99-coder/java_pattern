class Solution {
public:
    int maximum69Number(int num) {
        int mul = 1, last = 0;
        while (num / mul > 0) {
            if ((num / mul) % 10 == 6) last = mul;
            mul *= 10;
        }
        return last == 0 ? num : num + 3 * last;
    }
};
